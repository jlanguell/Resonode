py-cord[voice]>=2.6.1
faster-whisper>=1.0.3
python-dotenv>=1.0.1
numpy>=1.26

# Optional backends — install only what you enable in .env:
# deepgram-sdk>=3.0     # RESONODE_STT=deepgram   (cloud streaming STT, low latency, paid)
# openai>=1.0           # RESONODE_LLM=openai      (summaries / translation / moderation)
# anthropic>=0.40       # RESONODE_LLM=anthropic   (summaries / translation / moderation)
